/**
 * Frontend behavior for the {{ widget_name }} widget.
 *
 * Loaded on the frontend via get_script_depends(). To run inside the
 * Elementor editor preview too, hook elementor/frontend/init and use
 * elementorFrontend.hooks.addAction( 'frontend/element_ready/{{ widget_slug }}.default', fn ).
 */
( function ( $ ) {

    'use strict';

} )( jQuery );
